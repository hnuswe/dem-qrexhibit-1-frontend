import { Box, Button, Grid2, TextField } from "@mui/material";

function Visitor() {
	return (
		<Grid2 container spacing={3} direction={"column"} padding={2}>
			<Grid2 size={8}>Hello Visitor</Grid2>
		</Grid2>
	);
}

export default Visitor;
